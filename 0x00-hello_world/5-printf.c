#include <stdio.h>

/**
 * main - prints phrase "with proper grammar but
 * but outcome is piece of art"
 * Return: 0 if exited propperly, /0 otherwise
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art.\n");
	return (0);
}
